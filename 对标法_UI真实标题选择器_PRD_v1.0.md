# 双手剑系统｜对标法 UI 真实标题选择器 PRD v1.0

## 1. 背景与问题

对标法已经改为“根据业务定位、三家视角和账号人设寻找近 7 天真实标题，并直接使用原标题”，但当前页面仍以方法槽位和来源管理为中心。运营需要先理解刷新校验、补充来源、暂停生成等后台概念，才能完成真正任务：挑选一个适合当前账号的真实标题。

本次只重构对标法的前台信息架构和操作层级，不改变来源搜索、去重、时效、链接校验、标题保存、正文生成和历史数据结构。

## 2. 产品目标

1. 用户进入选题页时，默认第一眼看到对标法；原生法通过切换查看。
2. 真实原标题成为卡片的第一视觉信息，方法与技术数据退居辅助层。
3. 用户能快速看懂“为什么适合当前业务、视角和账号人设”。
4. 每张有效卡片只有一个主操作：选用这个标题。
5. 换批、单条换题、无结果和来源管理的层级清晰且不重复。

## 3. 页面结构

### 3.1 默认入口

- 进入步骤 3 时默认选中“对标法”。
- 切换业务、视角或账号人设后，步骤 3 仍默认回到“对标法”。
- 用户主动点击“原生法”后，本次当前页面允许继续查看原生法。

### 3.2 对标法顶部

- 标题：对标法。
- 说明：按当前业务、视角和账号人设筛选 7 天内真实标题，找到后直接使用原标题。
- 主操作：换一批真实标题。
- 默认/探索数量保留为弱提示。

### 3.3 有效标题卡

信息顺序：

1. 推荐序号与方法标签；
2. 真实原标题（最大字号，可编辑能力保留）；
3. 作者、平台、发布时间、热度与链接状态；
4. 匹配标签：业务、视角、账号人设；
5. 匹配说明；
6. 主操作“选用这个标题”；
7. 次操作“换一个”；
8. “来源详情”折叠区内保留原链接、刷新校验、补充来源及详细指标。

### 3.4 无结果卡

- 只显示一条状态，不重复出现“本轮暂停”和“暂停生成”。
- 文案：暂未找到符合当前业务、视角和账号人设的 7 天内真实标题。
- 操作：再找一次、手工添加。
- “系统只展示真实来源，不会虚构标题”作为低强调说明。

### 3.5 底部操作栏

- 未选择时：提示尚未选定；提供“换一批真实标题”。
- 已选择时：显示所选标题；提供“下一步：生成正文”。

## 4. 不在本次范围

- 不修改原生法布局和生成逻辑。
- 不修改对标来源检索、时效、去重与链接校验规则。
- 不新增数据库字段或 Supabase 迁移。
- 不改变正文、发布和复盘接口。
- 不恢复标题评分。

## 5. 交互与可访问性

- 所有主按钮至少 44px 高，键盘可聚焦。
- 动态找题状态使用 `aria-live`，且不能只用颜色表达。
- 正文标题、辅助信息和状态文字保持清晰对比度。
- 技术指标默认折叠，避免移动端首屏按钮拥挤。

## 6. 验收标准

1. 首次进入步骤 3 默认选中对标法；切换业务、视角、账号人设后仍成立。
2. 有效卡片第一视觉是原标题，并可在 5 秒内看见业务、视角和账号人设匹配依据。
3. 有效卡片的唯一主按钮是“选用这个标题”；单条换题为次按钮。
4. 来源详情默认收起，展开后原链接、刷新校验、补充来源和详细数据均可用。
5. 无结果槽位只出现一次状态说明，并可“再找一次”或“手工添加”。
6. 选中标题后底部出现已选标题与“下一步：生成正文”。
7. 原生法、对标法切换正常，原生法现有功能无回归。
8. 桌面端和手机端均无溢出、遮挡、重复主按钮或不可点击控件。
9. 类型检查、代码规范、测试和生产构建通过。
10. 上线后以真实用户路径完成多轮人眼检查；只做小规模 smoke，不进行生产压力测试。
