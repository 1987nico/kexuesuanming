# 正文生成门禁与操作者标注层：设计 QA

## 对比依据

- 现网视觉基准：`design-qa-source-section.png`
- 本地实现截图：`design-qa-local-section.png`
- 对比状态：均为「4 正文」步骤、已选定正文、浅色视觉；分别覆盖现网宽屏和本地窄屏响应式状态。
- 核心视觉目标：保留米白页面、白色大卡片、深蓝封面和轻量标签体系；把原来的结果卡片升级为正文内证据标注层，不重新设计品牌视觉。

## 对比结论

### 视觉与布局

- 保留原有信息顺序：方法标签 → 封面 → 标题 → 正文 → 复制与发布。
- 新增的「操作者校验层」使用现有浅灰表面、细边框和小圆角，没有引入新的重色大卡；在窄屏中按钮自动换行，无横向溢出。
- 三类证据颜色采用低饱和蓝、绿、黄，只突出正文中的依据句，不压过标题和正文层级。
- 正文保持纯文本阅读节奏；隐藏标注后不留占位，也不改变正文内容。

### 功能与状态

- 身份、兑现、转化植入三项必须全部通过并存在正文原文证据，正文才呈现给操作者。
- 未通过版本隐藏正文，显示失败项与「重新生成两个版本」，不能选定、复制或标记发布。
- 点击三类校验按钮会定位到对应原句，并显示本次判定依据。
- 「隐藏标注 / 显示标注」工作正常；隐藏后正文仍完整可读。
- 复制按钮仍只在选定最终正文后出现；浏览器实测复制内容包含正文和话题，不包含「操作者校验层」、校验依据或高亮标记。
- 数量校验只识别真正的清单承诺；“投了15家、3个AI面”等背景数字不会被误判为必须交付15项。
- 发布接口与选定接口都会再次执行三项门禁，前端绕过不能发布不合格正文。

### 可访问性与稳定性

- 标注开关和三类筛选均使用语义化按钮，并补充 `aria-pressed` 状态。
- 操作按钮保持至少 36px 高；核心复制和发布按钮保持至少 44px 高。
- 本地自动化浏览器控制台无错误。
- 类型检查、代码规范、107项测试和生产构建全部通过。

## 修正记录

1. 修正数字承诺识别，避免把投递数、薪资或日期误当清单数量。
2. 将预览样本的服务植入按买家、专家、商家分别表达，避免跨身份口吻。
3. 发布接口增加完整校验报告门禁，与正文选择入口保持一致。
4. 为标注开关增加可访问状态，并验证窄屏按钮换行和正文可读性。

final result: passed
