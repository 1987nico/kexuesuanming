# 双手剑系统｜对标法自适应来源池 PRD v1.0

## 1. 目标

在不降低标题与当前业务、三家视角和账号人设匹配度的前提下，提高对标法可用标题的供给量。系统不把“7天”机械改成“30天”，也不为了填满方法槽位展示低相关标题。

## 2. 核心原则

1. 对标法只使用有真实来源的原标题，不改写、不拼接、不由模型重新生成标题。
2. 匹配度优先于数量和新鲜度；跨业务、跨视角、跨账号人设的标题必须拦截。
3. 0—7天进入“近期机会”池。
4. 8—90天只有不含明显时效风险的标题才能进入“常青对标”池。
5. “今天、最新、本周、官宣、新规、年份、届数、倒计时”等时效型标题超过7天不得使用。
6. 不要求每个对标方法都有标题。没有高度匹配来源时只显示一次无结果状态，并引导切换原生法。
7. 标题和来源历史继续去重；失败批次不能替换现有可用标题。

## 3. 生成与推荐流程

```text
读取当前业务、视角、账号人设
→ 拉取真实来源候选
→ 业务硬过滤
→ 视角与账号身份过滤
→ 对标方法适配
→ 内容时效分类
   ├─ 0—7天：近期机会
   └─ 8—90天：常青对标（需通过时效风险检查）
→ 排除历史重复标题与来源
→ 按账号匹配度排序
→ 直接展示真实原标题
```

排序优先级：账号匹配度 > 方法适配度 > 近期/常青 > 热度与发布时间。前台不显示分数。

## 4. 前台体验

- 对标法仍是选题页默认入口。
- 顶部说明改为“近期优先，数量不足时自动补查常青对标”。
- 只渲染本轮真实可用标题，并按推荐顺序编号。
- 来源卡显示“7天内近期机会”或“常青对标”。
- 未找到标题的方法不显示空卡片，只在一个折叠摘要中说明。
- 全部无结果时只显示一个明确状态和“切换到原生法”按钮。
- 探索区同样不展示固定空槽位。

## 5. 数据与兼容

来源快照新增可选字段：

- `benchmark_pool`: `recent_opportunity | evergreen_benchmark`
- `source_validity_days`: `7 | 90`
- `source_match_score`: 后台排序信号

旧数据没有新字段时运行时自动推断，不强制执行数据库迁移。来源重新校验时重新计算分类，不能沿用过时分类。

## 6. 不在本次范围

- 蹭流量仍只允许7天内热点，不扩展到常青池。
- 不恢复标题评分。
- 不改变原生法生成逻辑。
- 不增加生产预热、批量压力测试或额外模型调用。

## 7. 验收标准

### 规则验收

1. 7天内真实标题通过其他来源与身份校验后可用。
2. 8—90天无时效风险且高度匹配的原标题可用，并标记“常青对标”。
3. 8—90天含年份、最新、本周、新规等时效表达的标题不可用。
4. 超过90天不可用。
5. 蹭流量超过7天仍不可用。
6. 标题必须等于来源原标题；业务、视角、账号人设不得串线。
7. 历史已出现的标题或来源不进入新批次。

### 用户体验验收

1. 桌面和手机端进入选题页第一眼仍是对标法。
2. 有一个合格标题就展示一个，不强行补足固定数量。
3. 无结果方法不出现空卡片；全部无结果时只有一处清晰说明。
4. 用户可一键切换原生法，不陷入死路。
5. 来源标签、原标题、作者、时间、热度和链接状态可读，无遮挡或溢出。
6. 连续切换业务、视角和账号人设后，标题不串线。

### 工程与上线验收

1. 类型检查、针对性测试、全量测试、代码规范检查和生产构建通过。
2. 本地以真实用户路径完成桌面与手机端人眼验收。
3. 上线后用独立新页面完成多轮生产 smoke；不进行压力测试，不反复消耗外部来源和模型额度。
4. 任一关键项失败则回滚到上线前不可变部署，不以接口成功代替人眼通过。
