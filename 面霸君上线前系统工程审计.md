# 面霸君上线前系统工程/API 审计

审计日期：2026-07-07
目标项目：`/Users/qifeng/Desktop/项目资料/职场参谋`
目标站点：`http://localhost:3001/mianba`
上线假设：Vercel + Supabase Cloud，生产环境所有真实数据必须云端持久化。

本文不记录任何密钥值，只列环境变量名、调用关系、风险和上线动作。

## 1. 结论先行

当前代码具备生产构建基础：

| 检查项 | 结果 | 说明 |
|---|---:|---|
| `npm run typecheck` | 通过 | TypeScript 类型检查通过。 |
| `npm run test` | 通过 | 14 个测试文件通过、3 个跳过；47 个测试通过、3 个跳过。 |
| `npm run build` | 通过 | Next.js 生产构建通过，API 路由均按动态函数构建。 |
| 本地 smoke test | 基本通过 | `/mianba` 未登录跳登录页，登录页可访问；受保护增长接口返回 401；公开模板/quick 接口可访问。 |

上线前最需要处理的不是构建问题，而是云端运行边界：

| 优先级 | 结论 | 上线动作 |
|---|---|---|
| P0 | Supabase 未配置时，部分模块会降级到本地文件或内存存储，生产数据可能丢失。 | 生产环境必须配置 Supabase 变量并验证 schema；建议生产缺 DB 时直接失败。 |
| P0/P1 | `/api/runs/*` 是公开链路，其中多步可触发 LLM 与 Tavily，公网部署后可能被刷成本。 | 上线生产前加鉴权、限流或移出公网入口。 |
| P1 | PDF、方向滑卡等公开接口以 `profileId` 为入口，能读取或触发生成报告内容。 | 使用签名链接/一次性 token/限流，并明确哪些字段允许匿名访问。 |
| P1 | `.env.example` 与实际代码读取的变量不完全一致。 | 整理环境变量清单，避免上线漏配后静默降级。 |
| P1 | `supabase/schema.sql` 里存在重复建表片段。 | 整理为可重复执行、无歧义的正式迁移。 |

## 2. 系统边界

| 层级 | 主要文件 | 职责 |
|---|---|---|
| 页面层 | `app/mianba/*`、`app/survey/directions/[profileId]/*`、`app/quick/*`、`app/run/*` | 用户登录、后台管理、报告交付、增长内容、公开问卷/验证链路。 |
| API 层 | `app/api/mianba/auth/*`、`app/api/reports/*`、`app/api/growth/*`、`app/api/quick/*`、`app/api/runs/*` | 处理鉴权、报告生成、内容生成、问卷数据、公开验证任务。 |
| 业务服务 | `lib/reports/*`、`lib/growth/*`、`lib/workflow/*`、`lib/auth/*` | 报告流水线、增长流水线、LLM 工作流、后台账号和租户权限。 |
| 数据存储 | `lib/db/supabase.ts`、`lib/reports/store.ts`、`lib/growth/store.ts`、`lib/db/store.ts` | Supabase 优先，部分模块无 Supabase 时降级到文件或内存。 |
| 外部服务 | Supabase、LLM Router、Tavily、OpenAI/火山图片、Chromium PDF、Vercel `waitUntil` | 云端持久化、文本生成、联网搜索、图片生成、PDF 渲染、后台异步预热。 |

## 3. API 调用清单

### 3.1 后台登录与权限：`mianba/auth`

| 前端页面/操作 | API 路由 | 读写数据 | 外部服务 | 鉴权方式 | 风险 |
|---|---|---|---|---|---|
| `/mianba/login` 提交登录 | `POST /api/mianba/auth/login` | 读取 `mianba_users`，写入 `mianba_auth_sessions` 或本地 auth 文件。 | Supabase；无 Supabase 时 FileStore。 | 账号密码校验，成功后设置 httpOnly cookie。 | 生产必须配置 `MIANBA_AUTH_SECRET` 和强密码；否则无法登录或存在弱口令风险。 |
| 后台退出 | `POST /api/mianba/auth/logout` | 删除当前 session。 | Supabase 或 FileStore。 | 依赖 cookie 中 session。 | 风险较低。 |
| 后台获取当前用户 | `GET /api/mianba/auth/me` | 读取 session 和用户。 | Supabase 或 FileStore。 | cookie session。 | 风险较低。 |
| 管理员用户管理 | `GET/POST/PATCH /api/mianba/auth/users` | 读取/创建/更新后台用户。 | Supabase 或 FileStore。 | `requireMianbaApiAuth(["admin"])`。 | 管理员接口已做 API 二次校验；上线需确认 admin 密码和角色。 |
| 页面访问 `/mianba/*` | `middleware.ts` | 不读写业务数据，仅检查 cookie。 | 无。 | 中间件拦截，放行 `/mianba/login` 与 `/mianba/reports/new`。 | 不能只依赖 middleware；关键 API 已大多二次校验，但公开接口需单列策略。 |

### 3.2 报告交付链路：`reports`

| 前端页面/操作 | API 路由 | 读写数据 | 外部服务 | 鉴权方式 | 风险 |
|---|---|---|---|---|---|
| `/mianba/reports/new` 读取天赋题库 | `GET /api/reports/talent-template` | 读取本地模板配置。 | 无。 | 公开。 | 可公开，风险低。 |
| `/mianba/reports/new` 提交底稿 | `POST /api/reports/assessment-profiles` | 写入 `assessment_profiles`，创建 lite order，异步预热初步诊断。 | Supabase/FileStore；LLM；Vercel `waitUntil`。 | 公开入口。 | 公开提交会写库并可能触发 LLM，需防刷、限流、表单风控。 |
| 后台报告列表 | `GET /api/reports/assessment-profiles?view=list` | 读取 profile 列表、订单摘要、租户范围。 | Supabase/FileStore。 | `requireMianbaApiAuth()`。 | 已做 API 鉴权。 |
| 后台转派负责人 | `PATCH /api/reports/assessment-profiles/[profileId]/owner` | 更新 `owner_user_id`。 | Supabase/FileStore。 | `requireMianbaApiAuth(["admin","operator"])`。 | 需确认 operator 只能操作权限范围内对象。 |
| 生成/刷新初步诊断 | `POST /api/reports/lite` | 读取 profile，写入 `initial_diagnosis`。 | LLM Router；Supabase/FileStore。 | `requireMianbaApiAuth()`。 | 成本接口，后台可用；应有日志和重试边界。 |
| 打开方向滑卡 | `POST /api/reports/direction-sessions` | 创建/更新方向 session，生成候选卡。 | LLM Router；Tavily；Supabase/FileStore。 | `requireMianbaApiAuth()`。 | 后台触发成本接口。 |
| 用户读取方向滑卡 | `GET /api/reports/direction-sessions/[profileId]` | 读取 session；必要时生成下一批卡。 | LLM Router；Tavily；Supabase/FileStore。 | 公开。 | 任何拿到 `profileId` 的人可访问候选方向，且可能触发生成成本。 |
| 用户滑动方向卡 | `POST /api/reports/direction-sessions/[profileId]/swipes` | 写入 liked/disliked 卡；满足条件后预热大报告。 | Supabase/FileStore；LLM；Tavily；Vercel `waitUntil`。 | 公开。 | 公开写入接口，需签名链接/限流/幂等保护。 |
| 后台生成深度报告 | `POST /api/reports/deep` | 读取 profile、初诊、滑卡结果；写入 `golden_report`。 | LLM Router；Tavily；Supabase/FileStore。 | `requireMianbaApiAuth()`。 | 成本较高，应记录生成状态和失败原因。 |
| 后台生成 golden 报告 | `POST /api/reports/golden/[profileId]` | 读取 profile 和方向 session，写入 golden report。 | LLM Router；Tavily。 | `requireMianbaApiAuth()`。 | 成本接口。 |
| 初步诊断 PDF | `GET /api/reports/initial/[profileId]/pdf` | 读取或生成初诊，写入 PDF 缓存。 | LLM Router；Chromium；Supabase Storage。 | 公开。 | 知道 `profileId` 即可拉取/生成 PDF，需签名或鉴权策略。 |
| 深度报告 PDF | `GET /api/reports/deep/[profileId]/pdf` | 读取或生成 golden report，写入 PDF 缓存。 | LLM Router；Tavily；Chromium；Supabase Storage。 | 公开。 | 公开接口可能触发昂贵生成链路，是上线重点风险。 |
| 订单列表/创建 | `GET/POST /api/reports/orders` | 读写 `report_orders`。 | Supabase/FileStore。 | 使用报告权限上下文；后台读取，公开新建依赖页面链路。 | 需确认公开创建订单不会被滥用。 |
| 订单详情/更新 | `GET/PATCH /api/reports/orders/[orderId]` | 读取/更新订单状态。 | Supabase/FileStore。 | 报告权限上下文。 | 状态更新需确认仅后台可改。 |

### 3.3 增长内容链路：`growth`

| 前端页面/操作 | API 路由 | 读写数据 | 外部服务 | 鉴权方式 | 风险 |
|---|---|---|---|---|---|
| 增长页初始化 | `GET /api/growth/bootstrap` | 读取账号、主题、草稿、设置。 | Supabase；无 Supabase 时 MemoryStore。 | `requireMianbaApiAuth()`。 | 无 Supabase 时数据只在进程内存在，重启丢失。 |
| 账号管理 | `/api/growth/account` | 读写增长账号资料。 | Supabase/MemoryStore。 | `requireMianbaApiAuth()`。 | 需确认租户隔离。 |
| 选题管理 | `/api/growth/topics` | 读写 topics。 | Supabase/MemoryStore。 | `requireMianbaApiAuth()`。 | 风险中低。 |
| 生成变体 | `/api/growth/topics/[topicId]/variants` | 读取 topic，写入候选内容。 | LLM Router。 | `requireMianbaApiAuth()`。 | 成本接口。 |
| 选择变体并成稿 | `/api/growth/topics/[topicId]/choose`、`/api/growth/drafts/[draftId]/review` | 写入草稿、阶段审核。 | LLM Router；Supabase/MemoryStore。 | `requireMianbaApiAuth()`。 | 成本和状态一致性风险。 |
| 标题评分/封面图 | `/api/growth/drafts/[draftId]/title-score`、`/api/growth/drafts/[draftId]/cover-image`、`/api/growth/covers/[imageId]` | 写入评分、图片任务或读取图片。 | LLM Router；OpenAI 图片或火山 Jimeng。 | `requireMianbaApiAuth()`。 | 图片生成成本较高；图片 provider/env 缺失时会失败或降级。 |
| 发布/审核 | `/api/growth/drafts/[draftId]/publish`、`/api/growth/stage-review` | 更新草稿状态和审核记录。 | Supabase/MemoryStore。 | `requireMianbaApiAuth()`。 | 需保留审计日志。 |
| 增长设置 | `GET/PUT /api/growth/settings` | 读写工作流设置。 | Supabase/MemoryStore。 | `requireMianbaApiAuth(["admin"])`。 | 只允许 admin，边界合理。 |

### 3.4 Quick 公开链路

| 前端页面/操作 | API 路由 | 读写数据 | 外部服务 | 鉴权方式 | 风险 |
|---|---|---|---|---|---|
| `/quick/[panel]` 读取面板 | `GET /api/quick/panels/[panelId]` | 读取 quick panel 配置。 | Supabase 或 MemoryStore。 | 公开。 | 适合公开问卷，但需确认没有敏感配置。 |
| 提交 respondent | `POST /api/quick/respondents` | 写入 respondent。 | Supabase 或 MemoryStore。 | 公开。 | 公开写入，需限流和垃圾提交治理。 |
| 提交/查询 scores | `POST/GET /api/quick/scores` | 写入/读取 quick scores。 | Supabase 或 MemoryStore。 | 公开。 | 需确认查询参数不会越权泄露他人分数。 |
| 查看聚合结果 | `GET /api/quick/aggregate` | 读取聚合。 | Supabase 或 MemoryStore。 | `OWNER_VIEW_TOKEN`。 | token 不是用户体系，需按部署环境保护。 |

### 3.5 Run 公开链路

| 前端页面/操作 | API 路由 | 读写数据 | 外部服务 | 鉴权方式 | 风险 |
|---|---|---|---|---|---|
| `/run` 新建分析 | `POST /api/runs` | 创建 run。 | Supabase 或 MemoryStore。 | 公开。 | 公开创建数据，需限流。 |
| `/run/[id]` 读取状态 | `GET /api/runs/[id]` | 读取 run、steps、scores。 | Supabase 或 MemoryStore。 | 公开。 | 知道 id 即可读取。 |
| 提交问卷 | `POST /api/runs/[id]/survey` | 写入 survey。 | Supabase 或 MemoryStore。 | 公开。 | 公开写入。 |
| Step1-2 生成 | `POST /api/runs/[id]/step1/generate`、`/step2/generate` | 读取问卷和前序步骤，写入 step 输出。 | LLM Router；Step2 使用 Tavily。 | 公开。 | 公网成本风险高。 |
| Step3 打分 | `POST/GET /api/runs/[id]/step3/scores` | 写入/读取方向评分。 | Supabase 或 MemoryStore。 | 公开。 | 可篡改 run 评分。 |
| Step4-6 生成 | `POST /api/runs/[id]/step4/generate`、`/step5/generate`、`/step6/generate` | 读取 Step2、Step3、Tavily 结果、VRIN 和 premortem，写入后续步骤。 | LLM Router；Tavily。 | 公开。 | 公网成本风险最高，应上线前收口。 |
| 事件/指标 | `/api/runs/[id]/events`、`/api/runs/[id]/metrics` | 写入/读取前端行为和指标。 | Supabase 或 MemoryStore。 | 公开。 | 需限制事件量，避免污染数据。 |

## 4. 外部服务清单

| 外部服务 | 调用文件 | 用途 | 缺环境变量时表现 | 上线要求 |
|---|---|---|---|---|
| Supabase | `lib/db/supabase.ts`、`lib/reports/store.ts`、`lib/growth/store.ts`、`lib/db/store.ts`、`lib/auth/mianbaStore.ts` | 用户/session、报告、订单、增长内容、quick/run 数据、PDF bucket。 | `supabaseServer()` 返回 `null`；报告/auth 可能 FileStore，growth/run/quick 可能 MemoryStore。 | 生产必须配置 `NEXT_PUBLIC_SUPABASE_URL`、`SUPABASE_SERVICE_ROLE_KEY`，并执行正式 schema。 |
| LLM Router | `lib/llm/router.ts`，由 `lib/reports/*`、`lib/growth/*`、`lib/workflow/runner.ts` 调用 | 初步诊断、方向卡、大报告、增长文案、标题评分、run steps。 | 若 provider key 缺失，对应生成会报错；有 fallback 时尝试备用 provider。 | 配置主备 provider、模型、超时和成本监控。 |
| Tavily | `lib/search/tavily.ts`，由报告深度流水线和 workflow Step2/4 调用 | 行业/趋势/机会方向搜索证据。 | 无 `TAVILY_API_KEY` 时返回空结果，不阻断流程但证据变弱。 | 生产建议配置，并记录搜索失败/空结果。 |
| OpenAI 图片 | `lib/image/router.ts` | 增长封面图生成。 | 无 `OPENAI_API_KEY` 时 OpenAI 图片生成失败。 | 若使用 OpenAI 图像，配置 `OPENAI_API_KEY`、可选 `OPENAI_BASE_URL`、`IMAGE_MODEL`。 |
| 火山 Jimeng 图片 | `lib/image/router.ts`、`lib/image/volcengine.ts` | 增长封面图生成备用/指定 provider。 | 无 `VOLCENGINE_AK`/`VOLCENGINE_SK` 时失败。 | 若使用火山，配置 `VOLCENGINE_AK`、`VOLCENGINE_SK`、可选 `VOLCENGINE_JIMENG_REQ_KEY`。 |
| Chromium PDF | `lib/reports/pdf.ts` | 将初步诊断/深度报告页面渲染为 PDF。 | Vercel 用 `@sparticuz/chromium`；本地需 `CHROME_PATH` 或系统 Chrome。缺失会导致 PDF 生成失败。 | Vercel 函数需足够内存/超时；确认 PDF 路由可启动 Chromium。 |
| Vercel `waitUntil` | `app/api/reports/assessment-profiles/route.ts`、`app/api/reports/direction-sessions/[profileId]/swipes/route.ts` 等 | 提交后异步预热初诊、方向卡、大报告。 | 非 Vercel 环境退化为普通 Promise/后台触发。 | 生产需观察后台任务失败日志，避免用户以为已生成但实际失败。 |
| 本地文件/内存 fallback | `.data/*`、MemoryStore | 本地开发时免数据库运行。 | 生产若误用，会丢数据或多实例不一致。 | 生产建议禁止 fallback，缺 DB 直接失败。 |

## 5. 信息引用关系清单

### 5.1 报告链路：结构化录入 → 价值观/天赋画像 → 初步诊断 → 方向滑卡 → 大报告 → PDF

| 步骤 | 产物 | 引用的信息/结论 | 下一步如何使用 |
|---|---|---|---|
| 结构化录入 | `AssessmentProfile` | 用户基本信息、问卷回答、价值观、252 天赋题、补充文本。 | 作为所有报告生成的根数据；写入 `assessment_profiles`。 |
| 价值观/天赋画像 | `valueProfile`、`talentProfile`、`buildLiteCustomerInput()` | 从 profile 中提取价值观排序、优势/短板、动机、约束、工作偏好。 | 初步诊断、大报告和方向卡都引用这些字段。 |
| 初步诊断 | `initial_diagnosis` | 引用结构化录入、价值观、天赋画像，生成 `recommended_cut`、核心判断、风险和建议。 | 方向滑卡读取 `recommended_cut` 和关键画像，决定首批方向候选。 |
| 方向滑卡 | `direction_session` | 引用 profile、价值观/天赋、初诊结论；每轮使用已喜欢/不喜欢的卡片调整生成。 | 收集用户真实偏好，形成 liked cards 和 rejected cards。 |
| 大报告生成 | `golden_report` | 引用 profile、初诊、liked cards；再通过 Tavily 补行业证据，生成 PEST、五力、VRIN、premortem 和最终建议。 | 作为深度报告页面和 PDF 的主要内容源。 |
| PDF 输出 | PDF 缓存/Storage 对象 | 初步 PDF 引用 `initial_diagnosis`；深度 PDF 引用 `golden_report`，缺失时可能触发生成。 | 交付用户下载/分享；建议公开链接签名化。 |

### 5.2 Run 链路：公开验证工作流

| 步骤 | 引用的信息/结论 | 产物 |
|---|---|---|
| 创建 run | 无前置信息，仅创建会话 id。 | `runs` 记录。 |
| 提交 survey | 用户问卷、PrinciplesYou 相关输入。 | run survey 数据。 |
| Step1 | survey + Principles 信息。 | 自我画像/输入归纳。 |
| Step2 | survey + Step1 + Tavily 搜索片段。 | 候选方向池。 |
| Step3 | 用户对 Step2 方向的评分。 | direction scores。 |
| Step4 | Step2 + Step3 中高分方向 + Tavily。 | 外部机会和市场证据。 |
| Step5 | Step4 shortlist + survey/principles。 | VRIN/个人匹配判断。 |
| Step6 | Top 方向 + Step4/5 + Step3 评分理由。 | premortem 和最终建议。 |

## 6. 上线风险清单

| 优先级 | 风险 | 影响 | 建议动作 |
|---|---|---|---|
| P0 | 生产未接 Supabase 时降级到 FileStore/MemoryStore。 | 多实例不一致、重启丢数据、用户报告不可恢复。 | 生产环境缺 `NEXT_PUBLIC_SUPABASE_URL` 或 `SUPABASE_SERVICE_ROLE_KEY` 时直接 fail fast。 |
| P0/P1 | `/api/runs/*/generate` 公网可触发 LLM/Tavily。 | 被刷成本、污染数据、占用函数并发。 | 加登录/签名 token/限流；若只是内部 demo，生产关闭。 |
| P1 | PDF 路由公开且可能触发报告生成。 | 泄露报告内容、产生高额 LLM/PDF 成本。 | 改为签名下载链接，或后台生成后只暴露短期 token。 |
| P1 | 方向滑卡 GET/POST 公开可读写。 | 他人可读取候选方向或篡改偏好。 | 每个 profile 生成独立 session token，接口校验 token。 |
| P1 | `.env.example` 未覆盖实际读取变量。 | 上线漏配后静默降级或功能失败。 | 将实际变量整理进 `.env.example` 和 Vercel checklist。 |
| P1 | `supabase/schema.sql` 有重复建表定义。 | 新 Supabase 项目迁移结果不清晰，后续维护易错。 | 拆成干净 migration，重复表只保留一份定义。 |
| P2 | 公开报告新建入口无明显限流。 | 垃圾 profile/订单、异步生成排队。 | 增加 IP/设备/验证码/队列限制。 |
| P2 | 中间件不能作为唯一安全边界。 | 未来 Next middleware 漏洞或绕过时，页面保护失效。 | 保持 API 内二次鉴权；公开 API 明确签名边界。 |
| P2 | PDF bucket 由代码尝试自动创建。 | 权限/策略不符合预期时运行时失败。 | 在 Supabase 后台预建 `report-pdfs` bucket 并验证读写策略。 |
| P2 | 本地默认 admin/operator 只在非生产种子。 | 若误将弱密码带到线上，后台风险高。 | 生产只使用强密码环境变量，并上线后轮换。 |

## 7. 环境变量核对

### 7.1 生产必须配置

| 变量名 | 用途 |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase 项目 URL。 |
| `SUPABASE_SERVICE_ROLE_KEY` | 服务端写库、Storage、后台数据访问。 |
| `MIANBA_AUTH_SECRET` | 后台 session 签名。 |
| `MIANBA_ADMIN_ACCOUNT`、`MIANBA_ADMIN_PASSWORD` | 管理员账号种子/登录。 |
| `MIANBA_OPERATOR_ACCOUNT`、`MIANBA_OPERATOR_PASSWORD` | 运营账号种子/登录，如使用 operator。 |
| `APP_BASE_URL` | PDF 页面渲染、站内绝对地址。 |
| 至少一组 LLM provider key | 如 `ANTHROPIC_API_KEY`、`OPENAI_API_KEY`、`DEEPSEEK_API_KEY`、`ARK_API_KEY`。 |

### 7.2 按功能配置

| 变量名 | 用途 |
|---|---|
| `LLM_PRIMARY_PROVIDER`、`LLM_PRIMARY_MODEL`、`LLM_FALLBACK_PROVIDER`、`LLM_FALLBACK_MODEL` | LLM 主备路由。 |
| `OPENAI_BASE_URL`、`ARK_BASE_URL`、`ARK_THINKING` | OpenAI 兼容或火山方舟模型路由。 |
| `TAVILY_API_KEY` | 报告和 run 链路的外部搜索证据。 |
| `IMAGE_PROVIDER`、`IMAGE_MODEL` | 增长封面图 provider/model。 |
| `VOLCENGINE_AK`、`VOLCENGINE_SK`、`VOLCENGINE_JIMENG_REQ_KEY` | 火山 Jimeng 图片生成。 |
| `OWNER_VIEW_TOKEN` | Quick 聚合页查看 token。 |
| `CHROME_PATH` | 本地 PDF 渲染 Chrome 路径；Vercel 通常走 `@sparticuz/chromium`。 |
| `PRINCIPLESYOU_COLLECT_PATH`、`GOLDEN_DEEP_FORCE_FIXTURE` | 本地/测试辅助变量，生产慎用。 |

`.env.example` 当前没有完整覆盖 `APP_BASE_URL`、`ARK_API_KEY`、`IMAGE_PROVIDER`、`IMAGE_MODEL`、`VOLCENGINE_AK`、`VOLCENGINE_SK`、`CHROME_PATH` 等实际可用变量；同时包含短信变量但当前审计范围内未看到主链路使用，应整理说明。

## 8. Supabase readiness

上线前请在 Supabase Cloud 做一次干净验证：

| 检查项 | 要求 |
|---|---|
| schema | 执行整理后的 `supabase/schema.sql` 或正式 migration。 |
| 后台权限表 | `tenants`、`mianba_users`、`mianba_auth_sessions`、`tenant_users` 可用。 |
| 报告表 | `assessment_profiles`、`report_orders`、方向 session、PDF cache 相关字段可用。 |
| 增长表 | `growth_accounts`、`growth_topics`、`growth_drafts`、`growth_stage_reviews`、图片/设置表可用。 |
| quick/run 表 | quick panels/respondents/scores、runs、steps、scores、events、usage metrics 可用或确认不上线。 |
| 负责人字段 | `owner_user_id` 及相关索引存在，后台列表权限正确。 |
| Storage | 预建或验证 `report-pdfs` bucket，确认服务端能写、前端访问方式符合隐私策略。 |

## 9. Next.js 安全公告核验

| 来源 | 与当前项目的关系 | 结论 |
|---|---|---|
| [Next.js Security Update: December 11, 2025](https://nextjs.org/blog/security-update-2025-12-11) | 当前项目使用 `next@14.2.35`。公告中的 14.x 修复版本包含 14.2.35。 | 当前版本满足该批 14.x 修复边界。 |
| [NVD CVE-2025-29927](https://nvd.nist.gov/vuln/detail/CVE-2025-29927) | 该漏洞是 middleware 授权绕过；14.x 修复版本为 14.2.25 及以上。 | 当前 `14.2.35` 已高于修复版本，但仍不应只靠 middleware。 |
| [Next.js CVE-2025-66478](https://nextjs.org/blog/CVE-2025-66478) | 官方说明 Next.js 14 稳定版不受该 React Server Components RCE 影响。 | 当前项目属于 14.2.x 稳定线，不在该 RCE 影响范围内。 |
| [GHSA-267c-6grr-h53f](https://github.com/vercel/next.js/security/advisories/GHSA-267c-6grr-h53f)、[GHSA-26hh-7cqf-hhc6](https://github.com/vercel/next.js/security/advisories/GHSA-26hh-7cqf-hhc6) | 2026 年 segment-prefetch/middleware 相关 advisories 主要影响 Next 15/16。 | 当前 Next 14 不在列出的影响区间；仍建议关键 API 做 route-level 鉴权。 |

## 10. 上线前动作清单

| 阶段 | 动作 | 验收标准 |
|---|---|---|
| 环境 | 在 Vercel Production 配齐 Supabase、auth、LLM、Tavily、图片、`APP_BASE_URL` 变量。 | 部署日志无缺变量降级提示；后台可登录。 |
| 数据库 | 在 Supabase Cloud 执行整理后的 schema/migration。 | 后台用户、报告、增长、PDF 相关表和 bucket 可读写。 |
| 安全 | 给公开成本接口加限流/签名/鉴权。 | 未授权用户不能触发 LLM/PDF 高成本链路。 |
| 构建 | 跑 `npm run typecheck`、`npm run test`、`npm run build`。 | 三项通过。 |
| Smoke test | 登录页、登录接口、`/mianba` 鉴权跳转、底稿提交、方向滑卡读取、PDF 路由、增长页 bootstrap。 | 每条链路返回预期状态，失败有可观察日志。 |
| 监控 | 打开 Vercel function logs、Supabase logs、LLM 成本监控。 | 能追踪每次报告/增长生成的失败原因和成本。 |
| 回滚 | 准备关闭公开 run/quick 或图片生成的开关。 | 成本异常时能快速止血。 |

## 11. 建议修复顺序

1. 先修 P0：生产缺 Supabase 时 fail fast；整理 Vercel Production env 和 Supabase schema。
2. 收口公开成本接口：优先处理 `/api/runs/*/generate`、公开 PDF、方向滑卡 POST。
3. 给公开报告交付链路加签名 token：底稿提交、方向滑卡、PDF 下载要区分“用户本人可访问”和“任何人可访问”。
4. 整理 `.env.example` 和上线 checklist：变量名、用途、缺失行为、是否必填。
5. 拆分并清理 Supabase schema：去掉重复建表，形成可重复迁移的正式版本。
6. 增加生产观测：生成任务状态、失败重试、LLM/图片调用成本、PDF 渲染失败日志。

## 12. 本次审计未执行的事项

| 未执行项 | 原因 |
|---|---|
| 修改业务 API | 本次任务是上线前审计，不做接口行为变更。 |
| 读取或记录真实密钥值 | 避免密钥进入审计文档。 |
| 操作生产 Vercel/Supabase | 本地无生产凭据确认授权；本报告给出 readiness checklist。 |
| 压测公开接口 | 需要上线域名、限流策略和成本预算后再执行。 |

## 13. 2026-07-07 修复落地记录

用户确认按第 11 节顺序进入修复阶段后，已完成以下代码修复：

| 修复项 | 落地结果 |
|---|---|
| 生产缺 Supabase fail fast | `reportStore`、`growthStore`、`store`、`mianbaAuthStore` 在生产环境缺 `NEXT_PUBLIC_SUPABASE_URL` 或 `SUPABASE_SERVICE_ROLE_KEY` 时拒绝启用本地/内存存储。 |
| 公开 run 成本接口收口 | `/api/runs/[id]/step1/2/4/5/6/generate` 生产环境默认关闭公开调用；后台登录或 `RUN_GENERATION_TOKEN` 才能触发。 |
| 报告/滑卡签名访问 | 新增 profile 级 HMAC 签名链接；`/reports/initial`、`/reports/lite`、`/reports/deep`、PDF API、方向滑卡 GET/POST 均要求后台登录或签名 token。 |
| 交付链接更新 | 底稿提交、报告交付后台、订单后台返回/复制的报告、PDF、方向滑卡链接均改为签名链接。 |
| 环境变量整理 | `.env.example` 补齐 `APP_BASE_URL`、方舟/火山/图片、`RUN_GENERATION_TOKEN`、PDF/本地辅助变量。 |
| Supabase schema 清理 | 删除文件开头重复的报告/增长/usage 建表块，保留后续带租户关系的正式定义和兼容 `alter table`。 |
| 生产观测 | 新增公开访问拒绝、run 生成门禁拒绝的结构化日志，便于 Vercel 日志追踪。 |

修复后验证：

| 检查项 | 结果 |
|---|---|
| `npm run typecheck` | 通过 |
| `npm run test` | 14 个测试文件通过、3 个跳过；47 个测试通过、3 个跳过 |
| `npm run build` | 通过 |
| 本地生产 smoke test | `/mianba` 未登录 307 跳登录；登录页 200；增长接口未登录 401；未签名方向 API 403；未签名 PDF 403；未签名报告页 404；生产 run 生成接口 403 |
