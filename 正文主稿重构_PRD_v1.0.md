# 双手剑系统｜正文主稿重构 PRD v1.0

## 1. 文档信息

- 产品：双手剑系统·面霸君版“小红书笔记”模块
- 模块：4 正文
- 版本：v1.0
- 状态：待实施
- 目标：把“短版＋长版”双稿流程重构为“结构化正文经营合同＋一篇可直接发布的主稿”
- 适用业务：留学生求职辅导、中高管职业决策
- 适用视角：商家、买家、专家

## 2. 背景与问题

当前正文流程已经具备以下能力：

- 业务、视角、账号人设隔离；
- 商家、买家、专家的基础经营规则；
- 身份、标题兑现和转化植入检查；
- 操作者标注层与干净复制；
- 标题、正文、话题总计不超过1000字；
- 历史正文去重；
- 短版先呈现、长版后台补齐。

但“双稿制”产生了明显的结构性成本：

1. 同一标题需要生成短版和长版，并分别校验；
2. 系统还需判断两版长度关系和内容差异；
3. 短版可能被长版误认为历史稿，长版也可能被短版误伤；
4. 任一版本失败都会产生额外修复、重试和等待；
5. 用户需要在两篇核心判断相同的正文中再次选择；
6. Token、CPU、缓存和历史数据量被放大；
7. 视角经营规则目前主要作为提示文本传递，尚未固化为本篇选题的结构化合同；
8. 重历史账号容易因为历史去重范围过大而无法稳定交付正文。

本次重构不降低身份、兑现、经营转化、合规和事实边界要求，而是减少重复生成和无价值选择。

## 3. 产品目标

### 3.1 核心目标

用户选定标题后，系统自动完成：

```text
当前业务定位
→ 当前商家/买家/专家视角
→ 当前账号人设
→ 当前标题与标题承诺
→ 编译正文经营合同
→ 生成一篇发布主稿
→ 内部字段级校验和修复
→ 展示可选择、可复制的最终正文
```

用户不需要：

- 选择内容方向；
- 选择正文阶段；
- 在短版和长版之间做二次决策；
- 理解或处理正文校验失败；
- 反复点击重新生成直到碰到合格结果。

### 3.2 商业目标

- 买家：先形成共鸣和信任，再促成有效咨询；
- 专家：通过比较、判断和选择标准促成有效咨询或诊断购买；
- 商家：围绕一个真实SKU促成产品咨询或购买；
- 三种视角最终统一观察有效咨询与成交，但承担不同的前置经营任务。

### 3.3 体验目标

- 正文页第一眼只呈现一篇系统推荐的发布主稿；
- 默认主稿无需再次加工即可复制发布；
- 用户需要时可以在同一篇正文上精简、补充或换写法；
- 调整失败时保留上一版可用正文；
- 生成失败不能留下空白页面。

## 4. 非目标

本次不做：

- 不恢复A/B/C内容方向；
- 不恢复正文阶段选择；
- 不增加正文评分；
- 不修改13种标题方法；
- 不修改“对标法直接使用真实原标题”的定义；
- 不复制对标原帖正文；
- 不修改0业务定位、1三家视角、2账号人设的主要信息架构；
- 不自动发布小红书；
- 不自动调整默认/探索方法关系；
- 不自动合并开放标签；
- 不删除或覆盖历史短版、长版正文。

## 5. 总体架构

```text
业务事实
  ↓
视角经营合同
  ↓
账号人设事实
  ↓
选定标题与标题承诺
  ↓
正文经营合同编译器
  ↓
发布主稿生成器
  ↓
字段级校验与局部修复
  ↓
操作者证据标注层
  ↓
干净复制与人工发布
  ↓
三日复盘
```

职责边界：

| 层级 | 控制内容 | 不控制内容 |
|---|---|---|
| 业务定位 | 产品、SKU、价格、交付、目标人群、事实与证据 | 不规定文章句式 |
| 三家视角 | 本篇经营任务和目标行为 | 不直接写死标题 |
| 账号人设 | 谁来讲、经历、口吻、可信边界 | 不跨业务或视角调用 |
| 选题 | 本篇谈什么、标题承诺、对象与冲突 | 不自由改变经营任务 |
| 正文 | 说服顺序、承诺兑现、证据和唯一承接动作 | 不重新改题 |
| 复盘 | 判断入口、正文执行与商业结果 | 不恢复内容方向配额 |

## 6. 正文经营合同

### 6.1 合同生成时机

用户在第3步选择标题后，进入第4步之前，由系统根据当前业务、视角、账号人设、标题、标题承诺和来源快照自动编译。

前台不要求用户先配置合同。

### 6.2 核心数据结构

```ts
type BodyContentIntent =
  | "buyer_experience_recommendation"
  | "buyer_problem_resonance"
  | "expert_category_comparison"
  | "expert_brand_comparison"
  | "expert_professional_judgement"
  | "merchant_sku_direct_sale"
  | "merchant_service_mechanism";

interface BodyOperatingContractV4 {
  version: "v4";
  business_line: GrowthBusinessLine;
  persona: GrowthPersona;
  account_id: string;
  profile_name: string;
  content_intent: BodyContentIntent;
  speaker_identity: string;
  subject: string;
  title: string;
  title_promise: string;
  required_evidence: string[];
  prohibited_claims: string[];
  conversion_goal: string;
  primary_action: string;
  active_sku_id?: string;
  comparison_type?: "category" | "brand";
  comparison_objects?: string[];
  evidence_mode?: "real_experience" | "authorized_composite" | "internal_training";
  source_snapshot_id?: string;
  compiled_at: string;
}
```

### 6.3 合同规则

- 合同必须保存当前业务、视角和账号ID，生成正文时再次核对；
- 切换业务、视角或账号后，旧合同不得继续使用；
- 用户编辑标题后，合同状态变为`stale`；
- 系统必须重新同步标题承诺和经营合同后才能生成正文；
- 对标法只使用真实原标题，正文只继承标题，不复制来源正文、案例、数字或表达；
- 找到的真实原标题若无法由当前业务和人设兑现，不得进入可选标题区；
- 合同是正文生成、修复、标注和复盘共同使用的唯一依据。

## 7. 三种视角的正文规则

### 7.1 买家

默认意图：经历推荐或问题共鸣。

必需结构：

```text
具体处境
→ 原有尝试
→ 为什么没有解决
→ 专业角色如何介入
→ 出现什么阶段变化
→ 克制推荐解决路径
→ 一个自然的站内咨询入口
```

硬性要求：

- 第一人称身份必须符合当前账号人设；
- 至少出现一个具体动作、现场、念头或对话；
- 专业服务必须自然进入因果链，不能突然硬植入；
- 必须同时交代介入动作和阶段变化；
- 不得直接报价或使用商家口吻；
- 真实亲历、授权复合案例和内部培训模拟必须遵守各自用途规则；
- 内部培训模拟正文不得进入正式发布和方法学习。

### 7.2 专家

默认意图：品类比较、品牌比较或专业判断。

品类比较结构：

```text
用户处境
→ 比较对象
→ 统一比较维度
→ 各自适合与不适合谁
→ 有条件的判断
→ 邀请用户补充个体变量
```

品牌比较额外要求：

- 保存公开来源；
- 记录利益关系；
- 不使用传闻或虚构竞品事实；
- 不恶意贬低；
- 不给脱离适用条件的绝对排名。

专家内容不能全部变成测评；系统仍需保留专业判断与方法拆解。

### 7.3 商家

默认意图：SKU直接售卖或服务机制说明。

必需结构：

```text
用户卡点
→ 一个具体SKU
→ 交付内容与过程
→ 价格和付款条件（仅在已配置时使用）
→ 信任依据
→ 适合与不适合谁
→ 一个购买或咨询动作
```

硬性要求：

- 每篇只能主推一个SKU；
- 每篇只能保留一个主要CTA；
- 未配置价格、付款方式或承诺时不得自行编造；
- 服务承诺不得超出SKU配置的条件；
- 不承诺确定上岸、确定录用或确定收益。

## 8. 一篇发布主稿

### 8.1 取消默认双稿

新流程停止默认生成：

- 短版；
- 长版；
- 短长版配对；
- 短长版长度差异检查；
- 短长版相似度检查；
- “等待长版后台补齐”。

系统只生成一个`selected`主稿。

### 8.2 自适应篇幅

系统根据标题承诺、经营意图和实际交付内容自动确定篇幅，不向用户提供固定字数选择。

建议目标区间，仅用于生成参考：

- 普通经历、判断或直售：约400—650字；
- 需要完整比较、清单或路线图：约550—800字；
- 标题＋正文＋话题总计始终不超过1000字。

字数区间不是发布门槛；只要内容完整、自然并符合1000字总限制即可。

### 8.3 主稿调整动作

主稿生成后提供：

- `再精简一些`：删除重复背景、重复解释和冗余情绪，不删除身份、标题兑现、关键证据、经营承接和CTA；
- `补充细节`：只增加一个现场、一组依据、必要步骤、阶段结果或避坑提醒；
- `换一种写法`：保持同一经营合同、标题主题、事实边界和CTA，重新组织开头、段落顺序和表达；
- `选定并复制`：确认当前主稿并显示复制标题、复制正文＋话题和完整发布包。

所有调整都形成同一`draft_lineage_id`下的新修订，不新增并列正文卡。

新修订通过完整校验后才替换当前主稿；失败时保留上一版。

## 9. 生成、校验与保障流程

### 9.1 正确流程

```text
编译经营合同
→ 生成正文骨架
→ 生成一篇主稿
→ 身份检查
→ 标题兑现检查
→ 经营任务检查
→ 唯一转化动作检查
→ 事实与合规检查
→ 1000字检查
→ 历史去重检查
→ 只修复失败字段
→ 再次完整校验
→ 展示最终主稿
```

### 9.2 检查项目

| 检查 | 通过标准 |
|---|---|
| 业务 | 不混入另一业务的人群、产品、场景或语言 |
| 身份 | 口吻、时间线和经历符合当前账号人设 |
| 兑现 | 标题数字、比较、清单、资料和结果承诺全部落实 |
| 经营任务 | 符合当前买家、专家或商家的经营结构 |
| 因果 | 问题、专业介入、动作和阶段变化形成完整链路 |
| 转化 | 只有一个主要承接动作，且自然出现在因果链中 |
| 事实 | 不自行编造账号定位和SKU未提供的事实 |
| 合规 | 不诱导互动、不违规导流、不做确定性成功承诺 |
| 字数 | 标题＋正文＋话题不超过1000字 |
| 去重 | 不与正式发布或正式选定历史正文实质重复 |

### 9.3 字段级修复

- 一个字段失败时，只修复相关段落；
- 每篇最多进行2轮自动字段修复；
- 修复后必须重新跑全部检查；
- 不得为了通过某项检查破坏已经通过的身份、兑现或经营任务；
- 不得把失败原因和修改工作交给普通操作者。

### 9.4 保障稿

若模型和两轮局部修复仍失败：

- 使用已经认证的经营合同和正文骨架确定性组装完整保障稿；
- 保障稿仍需通过业务、身份、兑现、经营任务、CTA、字数和去重检查；
- 用户端不得出现空白正文；
- 用户端不显示内部技术失败原因；
- 后台记录`fallback_used=true`和`incident_id`；
- 若保障稿也无法通过，保留标题选择和上一版正文，并提供一次明确的“再次生成”，不得清空已有内容。

## 10. 历史去重重构

### 10.1 三层历史

```text
硬去重历史：已发布正文＋运营正式选定正文
软去重历史：已经展示但未选定的主稿修订
排除历史：失败候选、内部修复中间稿、未展示保障候选
```

规则：

- 硬去重命中时不得交付；
- 软去重只用于指导模型更换开头、场景和叙事顺序，不得直接导致正文空白；
- 失败候选不得写入用户已看历史；
- 同一`draft_lineage_id`内的修订不得互相作为90天历史稿；
- 账号、业务、视角和人设必须进入去重隔离键；
- 不同账号不得互相污染正文去重历史。

建议隔离键：

```text
tenant_id
+ business_line
+ persona
+ account_id
+ content_intent
```

## 11. UI方案

### 11.1 页面结构

```text
4 正文

本篇任务：买家 · 经历推荐
标题承诺：讲清裸辞与在职验证的真实冲突
主要目标：促成有效咨询

发布主稿
系统已根据标题承诺自动确定篇幅

正文内容……

身份 1处｜兑现 3处｜经营承接 1处
[显示/隐藏标注]

[再精简一些] [补充细节] [换一种写法]
[选定并复制]
```

### 11.2 展示原则

- 第一眼只突出标题、主稿和主要动作；
- “本篇任务”只使用轻量标签，不增加新的生成前选择；
- 经营合同详情放入折叠区域；
- 操作者标注仅系统内可见；
- 复制内容永远只读取纯净标题、正文和话题；
- 旧短版、长版历史打开时继续按原格式只读展示。

### 11.3 状态文案

生成中：

```text
正在根据标题和当前人设生成发布主稿……
```

完成：

```text
发布主稿已经完成，可以直接查看和复制。
```

局部调整中：

```text
正在保留标题和核心判断，调整当前正文……
```

调整失败：

```text
这次调整没有形成更可靠的版本，当前可用正文已保留。
```

禁止再出现：

- 两个版本差异不足；
- 长版被短版误判重复；
- 等待长版后台补齐；
- 正文暂不呈现；
- 让操作者处理合同字段失败。

## 12. 接口改造

建议接口：

```text
POST /api/growth/body-contracts/compile
POST /api/growth/drafts/generate-main
POST /api/growth/drafts/:id/refine
POST /api/growth/drafts/:id/choose
```

`generate-main`请求至少包含：

```json
{
  "account_id": "account-id",
  "run_id": "run-id",
  "topic_id": "topic-id",
  "contract_version": "v4"
}
```

`refine`请求：

```json
{
  "draft_id": "draft-id",
  "action": "compact | enrich | rewrite",
  "base_revision": 1
}
```

接口要求：

- 同一账号、标题和合同版本使用幂等键；
- 重复点击不能重复创建多篇主稿；
- 正在进行的相同请求使用单飞锁；
- 调整请求未通过前不得覆盖当前正文；
- 用户切换业务、视角或账号后，迟到响应不得写入当前页面；
- 返回结果必须携带业务、视角、账号、标题和合同版本，供前端二次核对。

## 13. 数据与兼容

### 13.1 新字段

建议在新正文记录中增加：

```text
body_contract_version
body_operating_contract
content_intent
draft_lineage_id
revision_number
revision_action
generation_mode = main_draft
fallback_used
incident_id
generation_duration_ms
model_call_count
repair_count
```

新正文统一写入：

```text
selected_body_version = selected
```

### 13.2 历史兼容

- 历史`short`和`long`继续读取；
- 已选历史正文继续复制、发布和复盘；
- 不批量改写历史正文；
- 不把历史短长两版强行合并；
- 新流程不再新建`short`和`long`记录；
- 缓存键增加正文合同版本，旧双稿缓存不能命中新主稿流程；
- 历史复盘指标不丢失；
- 迁移SQL上线前准备，生产执行前必须备份并确认回滚路径。

## 14. 复盘调整

三日复盘增加以下描述性维度：

- 当前业务；
- 当前视角；
- 当前账号人设；
- 原生法或对标法；
- 标题方法；
- `content_intent`；
- 主稿是否经过精简、补充或换写法；
- 最终正文长度；
- 有效咨询、诊断购买和成交。

不同视角优先观察：

| 视角 | 前置指标 | 商业指标 |
|---|---|---|
| 买家 | 阅读时长、共鸣反馈、解决路径追问 | 有效咨询、成交 |
| 专家 | 收藏、分享、专业问题、变量提交 | 诊断购买、有效咨询、成交 |
| 商家 | SKU询价、服务入口访问 | 购买、有效咨询、成交 |

30篇新有效样本前只做描述性统计，不自动修改正文经营规则。

## 15. 性能与资源要求

以下为上线验收目标，不代表当前已实现数据：

- 主稿冷生成P50不超过12秒；
- 主稿冷生成P90不超过25秒；
- 缓存命中时P50不超过3秒；
- 正常主稿尽量只调用1次正文模型；
- 字段级修复最多2轮；
- 仅当主请求超过设定阈值时才允许启动备用请求；
- 主请求完成后必须取消未启动的备用请求；
- 页面空闲、查看正文、展开标注和复制时不得触发新模型请求；
- 不预生成同一标题的短版和长版；
- 不按账号批量预生成100篇完整正文；
- 生产环境不进行大批量压力测试。

## 16. 埋点与监控

新增或调整事件：

```text
body_contract_compiled
main_draft_requested
main_draft_generated
main_draft_shown
main_draft_refined
main_draft_chosen
main_draft_copied
main_draft_failed
main_draft_fallback_used
```

每次生成记录：

```text
合同编译耗时
模型生成耗时
校验耗时
修复耗时
去重耗时
总耗时
模型调用次数
修复次数
是否使用保障稿
失败字段
```

不得保存生产正文、账号敏感字段或模型密钥到公开日志。

## 17. 实施顺序

### P0：合同与单稿主链路

- 增加`BodyOperatingContractV4`；
- 标题选定后编译合同；
- 新流程只生成一篇`selected`主稿；
- 将视角经营合同结构化接入正文生成；
- 保留身份、兑现、经营、CTA、合规和字数检查；
- 缓存版本隔离旧双稿流程。

### P1：正文调整与保障交付

- 增加精简、补充细节、换写法；
- 增加同一正文修订链；
- 实现字段级修复；
- 实现确定性保障稿；
- 改造硬去重、软去重和失败候选隔离。

### P2：复盘与数据学习

- 三日复盘增加`content_intent`；
- 统计正文调整行为和最终篇幅；
- 30篇有效样本后提出描述性建议；
- 不自动改变视角经营规则。

## 18. 自动测试

必须覆盖：

1. 两条业务和三种视角的合同编译；
2. 业务、视角、账号切换后合同失效；
3. 用户改标题后合同变为`stale`；
4. 买家完整因果链；
5. 专家品类比较与品牌比较；
6. 商家单SKU和单CTA；
7. 未配置价格、付款方式时不能编造；
8. 对标标题只继承标题，不复制来源正文；
9. 主稿字数与1000字发布限制；
10. 字段级修复不破坏已通过字段；
11. 调整失败保留当前主稿；
12. 保障稿能够通过完整检查；
13. 失败候选不进入用户历史；
14. 同一修订链不互相误判历史重复；
15. 不同业务、视角和账号的缓存严格隔离；
16. 重复点击生成不产生重复主稿；
17. 迟到响应不写入已切换的账号页面；
18. 历史短版、长版仍可读取、复制和复盘。

## 19. 用户端人眼验收

自动测试通过后，必须从真实用户视角完成多轮操作，不能只看接口200或测试结果。

### 19.1 隔离预览验收

至少完成：

```text
2条业务 × 3种视角 × 每个视角2个标题 = 12篇主稿
```

每个视角至少包含一个原生法标题和一个对标法真实原标题。

逐篇检查：

- 业务、视角、账号人设是否对位；
- 正文是否完整兑现标题；
- 是否保持当前账号口吻和时间线；
- 买家、专家、商家的文章是否明显不同；
- 是否只有一个主要承接动作；
- 是否存在机械开头、硬植入、半截清单、断句或拼接感；
- 是否出现另一业务、另一视角、另一账号的素材；
- 对标正文是否复制来源正文；
- 标题＋正文＋话题是否不超过1000字；
- 系统是否只呈现一篇主稿；
- 精简、补充和换写法是否不改题；
- 复制内容是否不包含系统标注；
- 失败调整是否保留原主稿。

每个账号连续生成至少2篇，检查正文是否出现同开头、同场景、同因果链的换皮重复。

### 19.2 生产冒烟验收

生产只做有限验证：

- 留学生业务：买家、专家各1篇；
- 中高管业务：买家、专家、商家各1篇；
- 至少完成一次精简和一次补充细节；
- 至少完成一次选定、复制和发布包检查；
- 观察Vercel请求数量、CPU、失败率和主要耗时；
- 不在生产做30批、50批压力测试。

### 19.3 通过标准

- 新流程100%只展示一篇发布主稿；
- 业务、视角、账号人设零串线；
- 12篇隔离预览稿全部兑现标题承诺；
- 三种视角的经营结构人眼可区分；
- 商家100%单SKU、单CTA；
- 专家比较正文100%包含比较对象、统一维度和适用条件；
- 买家正文100%包含处境、尝试、介入和阶段变化；
- 所有正文符合1000字发布限制；
- 不出现空白正文或让操作者处理内部失败；
- 不出现短长版差异、配对或等待文案；
- 调整失败不覆盖当前可用正文；
- 生产冒烟期间无请求风暴和跨账号缓存命中；
- 桌面端和手机端均可完成完整操作。

若任一项失败：定位根因，进行最小修改，在隔离预览重测；全部通过后再做一次生产冒烟。禁止在生产无限循环修改和压力验证。

## 20. 上线与回滚

上线前：

1. 记录当前生产部署ID和代码提交，建立回滚基线；
2. 备份涉及的Supabase表结构与迁移SQL；
3. 使用功能开关控制新正文主稿流程；
4. 先对测试账号和少量账号开放；
5. 验证历史正文读取和新主稿写入同时正常。

建议功能开关：

```text
GROWTH_MAIN_DRAFT_V4_ENABLED
```

回滚时：

- 关闭功能开关，恢复旧双稿读取和生成；
- 保留已生成的`selected`主稿，不删除数据；
- 新字段继续只读保留；
- 不反向批量转换为短版或长版；
- 正式域名回指上线前部署。

## 21. 最终交付要求

实施完成后只汇报：

1. 修改范围；
2. 数据结构与兼容方式；
3. 自动测试结果；
4. 12篇隔离预览的人眼检查结果；
5. 5篇生产冒烟覆盖的业务、视角和账号；
6. 主稿生成P50、P90和模型调用次数；
7. 是否发现串线、重复、硬植入、断句或不兑现；
8. Vercel和Supabase资源变化；
9. 当前生产部署ID；
10. 回滚部署ID与回滚方式；
11. 尚未解决的风险。

没有完成真实用户端人眼验收，不得宣称上线完成。

## 22. 默认决策

- 取消默认短版和长版，改为一篇发布主稿；
- 主稿篇幅由标题承诺和经营意图自动决定；
- 不增加生成前的内容方向选择；
- 视角经营任务由系统自动继承；
- 商家每篇只售卖一个已配置SKU；
- 专家品类比较默认开放，品牌比较必须使用公开依据并说明利益关系；
- 买家优先使用真实亲历或授权复合案例；内部培训模拟不得对外发布；
- 身份、兑现、经营、唯一CTA、事实、合规、字数和硬去重继续作为交付门槛；
- 系统负责内部修复，普通操作者只看到最终主稿；
- 历史短版和长版永久保留并继续兼容；
- 本次重构质量优先，性能优化不能通过降低正文标准实现。
